apiVersion: v1
kind: Service
metadata:
  name: agentmesh
  namespace: agentmesh
spec:
  selector:
    app: agentmesh
  ports:
  - name: proxy
    port: 4000
    targetPort: 4000
  - name: api
    port: 4001
    targetPort: 4001
